#include <stdio.h>

int  testArray();

void feedArray(int arr[]);

void main() {
  feedArray();
  printf("%d"feedArray());
}

int feedArray(int arr[]) {
  for (int i = 1; i <= 5; i++) {
    int newArray[] = int arr[i];
    
  }
}